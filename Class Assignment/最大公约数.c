#include<stdio.h>

int main() {
	//枚举法
	//int a, b;
	//int min;

	//scanf_s("%d %d", &a, &b);
	//int min = a < b ? a : b;
	//int ret = 0;
	//int i;
	//for (i = 1; i <= min; i++) {
		//if (a % i == 0 && b % i == 0) {
			//ret = i;
		//}
	//}
	//printf("%d和%d的最大公约数是%d\n", a, b, ret);

	//辗转相除法--效率更高--编程的意思(a,b)=(b,a%b)，数学上是(a,b)=(b-a,b)


	return 0;
}